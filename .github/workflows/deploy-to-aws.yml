name: Deploy to AWS EC2

on:
  push:
    branches: [ "master" ]

env:
  EC2_IP: "54.180.93.68"
  EC2_USER: "ec2-user"
  DOCKER_IMAGE: "charge-station"
  MYSQL_CONTAINER: "mysql"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build with Maven
        run: |
          mvn clean package -DskipTests
          ls -l target/*.jar

      - name: Build and Validate Image
        run: |
          docker build -t ${{ env.DOCKER_IMAGE }} .
          docker save ${{ env.DOCKER_IMAGE }} -o /tmp/image.tar
          ls -lh /tmp/image.tar || { echo "[ERROR] 镜像导出失败"; exit 1; }
          md5sum /tmp/image.tar > /tmp/image.md5
          echo "镜像构建完成，大小: $(ls -lh /tmp/image.tar)"

      - name: Prepare SSH Environment
        run: |
          echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > /tmp/key.pem
          chmod 600 /tmp/key.pem
          # 禁用严格主机检查
          echo -e "Host *\n  StrictHostKeyChecking no" > /tmp/ssh_config

      - name: Guaranteed Transfer
        run: |
          # 分块传输（避免大文件中断）
          split -b 50M /tmp/image.tar /tmp/image.part.
          scp -F /tmp/ssh_config -i /tmp/key.pem /tmp/image.part.* ec2-user@${{ env.EC2_IP }}:/tmp/

          # 传输校验文件
          scp -F /tmp/ssh_config -i /tmp/key.pem /tmp/image.md5 ec2-user@${{ env.EC2_IP }}:/tmp/

          # 远程合并验证
          ssh -F /tmp/ssh_config -i /tmp/key.pem ec2-user@${{ env.EC2_IP }} "
            echo '正在合并分块文件...'
            cat /tmp/image.part.* > /tmp/image.tar
            rm -f /tmp/image.part.*
            echo '合并完成，校验文件...'
            md5sum -c /tmp/image.md5 || { echo '[ERROR] 文件校验失败'; exit 1; }
            echo '文件校验通过，大小: $(ls -lh /tmp/image.tar)'
          "

      - name: Deploy Services
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ env.EC2_IP }}
          username: ${{ env.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            # 1. 清理环境
            docker rm -f ${{ env.MYSQL_CONTAINER }} ${{ env.DOCKER_IMAGE }} || true
            docker network rm app-net || true

            # 2. 加载镜像（严格检查）
            echo "正在加载镜像..."
            docker load -i /tmp/image.tar || { echo '[ERROR] 镜像加载失败'; exit 1; }
            docker images | grep ${{ env.DOCKER_IMAGE }} || { echo '[ERROR] 镜像不存在'; exit 1; }

            # 3. 启动服务
            echo "创建网络..."
            docker network create app-net

            echo "启动MySQL..."
            docker run -d \
              --network app-net \
              --name ${{ env.MYSQL_CONTAINER }} \
              -e MYSQL_ROOT_PASSWORD=root \
              -e MYSQL_DATABASE=charge_station \
              -v mysql_data:/var/lib/mysql \
              mysql:8.0

            echo "启动应用..."
            docker run -d \
              --network app-net \
              -p 8080:8080 \
              --name ${{ env.DOCKER_IMAGE }} \
              ${{ env.DOCKER_IMAGE }}

            # 4. 健康检查（带实时日志）
            echo "等待应用启动..."
            for i in {1..2}; do
              if docker logs ${{ env.DOCKER_IMAGE }} 2>&1 | grep "Started Application"; then
                echo "应用启动成功！"
                curl -fsS http://localhost:8080/actuator/health && exit 0
              fi
              echo "[尝试 $i/12] 等待5秒..."
              sleep 5
            done

            echo "[ERROR] 应用启动超时，最后50行日志："
            docker logs ${{ env.DOCKER_IMAGE }} --tail 50
            exit 1