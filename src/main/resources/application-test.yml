# 测试环境配置
spring:
  datasource:
    url: jdbc:mysql://test-db:3306/charge_station?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true&allowMultiQueries=true&rewriteBatchedStatements=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    root: WARN  # 测试环境减少日志量
    com.emsp.chargestation: INFO

server:
  port: 8000