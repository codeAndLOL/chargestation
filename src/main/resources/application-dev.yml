# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/charge_station?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true&allowMultiQueries=true&rewriteBatchedStatements=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    root: INFO
    com.emsp.chargestation: DEBUG  # 开发环境开启DEBUG日志

server:
  port: 80